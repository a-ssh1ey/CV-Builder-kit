<style>
    .profile-section {
        padding: 20px;
        margin: 20px 0;
        background-color: var(--color-muted);
        color: var(--color-primary);
        font-size: var(--font-size-medium);
    }
</style>
<div class="profile-section">
    <i>{{local.profile.text}}</i>
</div>